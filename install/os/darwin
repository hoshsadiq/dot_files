kubectlSuffix="darwin/amd64/kubectl.exe"

brew install byobu
brew install git-crypt

echo "export JAVA_HOME='$(/usr/libexec/java_home)'" >> $HOME/.zshrc.local
echo 'addpath $HOME/Library/Python/3.6/bin after # pip install --user' >> $HOME/.zshrc.local

echo "Implement MacOS"
exit 1;
