# usage:
# - cat myfile C
# - cat myfile.json J
# if the letters are needed as real arguments, simply put them between quotes
alias -g C='| clipcopy'
alias -g L='| less -R'
alias -g G='| grep'
alias -g J='| jq'
